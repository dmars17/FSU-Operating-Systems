/*
1. Accept input
    - command line
    - file
2. parse input into commands
    - once by ';' -> individual full command (eg. ["ls -al"])
    - next by whitespace -> individual parsed command (eg. ["ls", "-al"])
3. execute each command
    - concurrent execution
    - execute in a loop
    - wait

if you are the parent you recieve the pid of the child. store it and do something later.


*/
